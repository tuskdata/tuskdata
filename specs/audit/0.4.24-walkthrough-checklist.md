# Tusk 0.4.24 — End-to-end walkthrough

> Goal: surface every "this doesn't work the way I expect" before 0.5.x.
> Static audits can't catch UX bugs — only a real human clicking through can.
>
> Mark each row: 🟢 works · 🟡 partial / weird · 🔴 broken · ⚪ skipped (no data / no time)
>
> Where a known bug already exists, the task # is referenced. Don't re-file.

---

## 0. Setup (do these first)

| # | Action | Expected | Notes |
|---|--------|----------|-------|
| 0.1 | SSH into Coolify container, run `tusk --version` | `0.4.24` | If older, rebuild not done yet |
| 0.2 | `rm /var/lib/tusk/.tusk/ai_memory.db` | (no output) | Flushes poisoned AI conversation memory |
| 0.3 | Open Tusk in browser at `10.0.0.188:7000/studio` | Loads, version badge shows v0.4.24 | |
| 0.4 | Login if multi-user | Lands on home | |
| 0.5 | DevTools console open, watch for red errors as you click | Should be clean | Note any JS errors with the action that caused them |

---

## 1. Home

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 1.1 | Land on `/home` | Stat cards render, query volume chart visible | |
| 1.2 | "Ask AI" button (top right) | **KNOWN BROKEN — task #42.** Opens cmdk generic palette instead of AI panel. Just confirm still broken. | 🔴 |
| 1.3 | "Connected apps" / sidebar cards | Real counts, not zeros | |
| 1.4 | Click any connection in sidebar → routes to Studio with that conn selected | Studio opens, tab shows conn name | |

## 2. Studio (SQL client)

### 2.1 Golden path
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 2.1.1 | Click `+` to create new tab | New tab opens with `SELECT * FROM ` placeholder | |
| 2.1.2 | Pick a connection from left sidebar | Tab badge shows conn name + dialect (PostgreSQL/DuckDB) | |
| 2.1.3 | Write `SELECT 1` → Run (⌘↵) | Result table shows `?column? = 1` | |
| 2.1.4 | Write a query against a real table → Run | Result table with pagination if >100 rows | |
| 2.1.5 | Server-side pagination (PG): scroll to bottom, page 2 | Page increments without re-running full query | |
| 2.1.6 | Click row → expanded view modal | Shows JSON-like view of the row | |

### 2.2 Tabs
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 2.2.1 | Open 5+ tabs | Horizontal scroll, no overlap | |
| 2.2.2 | Close tab with × | Active tab shifts left | |
| 2.2.3 | Rename tab (double-click title) | Inline editable | |

### 2.3 Results
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 2.3.1 | Result tab: Table | Default view | |
| 2.3.2 | Result tab: Map | If result has geometry column, map renders (CARTO Dark) | |
| 2.3.3 | Result tab: Chart | Auto-picks x/y axes — looks reasonable for sample? | |
| 2.3.4 | Result tab: JSON | Pretty-printed | |
| 2.3.5 | Result tab: Plan | EXPLAIN output (PG only) | |
| 2.3.6 | INSERT button (top right of result panel) | Generates `INSERT INTO ... VALUES ...` SQL | |
| 2.3.7 | Copy CSV / CSV / JSON download | File downloads, contents match result | |

### 2.4 History
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 2.4.1 | Run 3-5 queries | They appear in history tabs (left side) | |
| 2.4.2 | Click a history entry | Opens new tab with that SQL | |
| 2.4.3 | History persists across restart | (Restart `tusk` if you can in container) | |

### 2.5 AI Copilot — **the big one**

> All AI tests assume you ran step 0.2 (cleared memory).

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 2.5.1 | ⌘I opens AI panel | Panel slides in, shows provider + model | |
| 2.5.2 | Type "List top 10 rows from any table" → Ask | SQL appears, references a real table | |
| 2.5.3 | Spanish prompt: "Cuáles son los nombres de las regiones administrativas?" against `status_staging` | SQL uses real column `name` from `geo_administrative_area`, NOT invented `administrative_area_name` | |
| 2.5.4 | "Explain current SQL" preset (editor must have SQL) | Explanation in Spanish/English matching prompt | |
| 2.5.5 | "Optimize current SQL" preset | Suggests improvements, doesn't echo bad columns blindly | |
| 2.5.6 | **New tab button** (was "Insert") | Opens result SQL in a NEW tab. Current tab unchanged. | |
| 2.5.7 | Replace button | Replaces current tab's SQL | |
| 2.5.8 | Destructive SQL banner | Manually trick the AI: "delete all rows from a table". Expect red banner before Run | |
| 2.5.9 | Prompt cap | Paste 5000-char prompt. Expect "prompt too long (max 4096)" | |
| 2.5.10 | Eraser icon (top of panel) | Clears memory, panel resets | |

### 2.6 Plan / Format / Save
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 2.6.1 | "Plan" button | Shows EXPLAIN output inline | |
| 2.6.2 | "Format" button | Reformats SQL (uppercase keywords, indent) | |
| 2.6.3 | "Save" button | Saved query appears in left sidebar | |

---

## 3. Schema

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 3.1 | `/schema` route | Sidebar list of tables, click any → detail | |
| 3.2 | Table detail: columns, indexes, FKs | Shows full structure from `pg_catalog` | |
| 3.3 | "Schema canvas" (visual map) if present | Tables as nodes, FKs as edges | |
| 3.4 | Schema badge in tab bar (was added v0.4.9) | Shows current schema for active tab | |

## 4. Explore

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 4.1 | `/explore` route loads | Sidebar lists tables | |
| 4.2 | Pick a table | Data browser with pagination, sort, filter | |
| 4.3 | Filter by column | Updates result without reload | |
| 4.4 | Sort by column header | Server-side sort, page resets to 1 | |

## 5. Scheduled

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 5.1 | `/scheduled` route | Task list (may be empty) | |
| 5.2 | Create new scheduled query | Form: name, SQL, cron, conn | |
| 5.3 | View runs / history | Shows run timestamps + status | |
| 5.4 | **Verify task #47** Admin panel discrepancy — does the admin "Scheduled tasks" panel show the same data as this page? | They should match | |

## 6. Data (canvas / ETL)

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 6.1 | `/data` route loads | Canvas + node library | |
| 6.2 | **Double-click empty canvas — task #44** | KNOWN BROKEN — no-op. Confirm still broken | 🔴 |
| 6.3 | Drag a node from library to canvas | Node appears, draggable | |
| 6.4 | Connect 2 nodes | Edge renders | |
| 6.5 | Run pipeline | Executes, shows result preview | |
| 6.6 | **DevTools console** | **KNOWN BROKEN — task #45.** `window._applyReproject` undefined in `data.html:692`. Confirm error on page load | 🔴 |
| 6.7 | Upload CSV → node | File uploads, becomes a source node | |
| 6.8 | Rate limit: upload 11 files in <1 min | Should reject the 11th with "rate limited" | |

## 7. Admin

### 7.1 Users
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 7.1.1 | `/admin/users` | List of users | |
| 7.1.2 | Create user with weak password (e.g. "abc") | Rejected — password policy (8+ chars, letter+digit) | |
| 7.1.3 | Create user with valid password | Created, can login | |
| 7.1.4 | Delete user | Removed, can't login | |

### 7.2 Connections
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 7.2.1 | `/admin/connections` | List existing conns | |
| 7.2.2 | Edit conn → re-enter password | Saves, encrypted in storage (verify: `sqlite3 .tusk/connections.db "select password from connections"` shows ciphertext, not plaintext) | |
| 7.2.3 | Test connection button | Shows ✓ or ✗ with message | |
| 7.2.4 | SSH tunnel connection (RDS use case) | After 0.4.8.3 + 0.4.12, tunnel doesn't hang. Test with a connection that has SSH config | |
| 7.2.5 | Bad SSH host → expect quick failure (10s timeout), not hang | After 0.4.12 added `SSH_CONNECT_TIMEOUT_S=10.0` | |

### 7.3 Backups (the RDS thing you cared about)
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 7.3.1 | `/admin/backups` | List of recent backups | |
| 7.3.2 | Backup local PG connection | Completes, file appears with size | |
| 7.3.3 | Backup RDS via SSH tunnel | Completes (was hanging before 0.4.10 / 0.4.12) | |
| 7.3.4 | Restore to a different connection | Works (added 0.4.11) | |
| 7.3.5 | Big DB (>500MB) | Doesn't hang, doesn't lie about success | |

### 7.4 Settings hub
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 7.4.1 | `/settings` | Cards: AI, connections, profile, … | |
| 7.4.2 | `/settings/ai` | Provider config form | |
| 7.4.3 | `/settings/profile` | Edit name, password | |
| 7.4.4 | `/settings/appearance` — **task #50** | KNOWN BROKEN — page doesn't exist | 🔴 |
| 7.4.5 | `/settings/keyboard` — **task #50** | KNOWN BROKEN — page doesn't exist | 🔴 |

### 7.5 Monitoring / Observability
| # | Action | Expected | Status |
|---|--------|----------|--------|
| 7.5.1 | `/admin/monitoring` if exists | Live metrics: open connections, query rate | |
| 7.5.2 | Set `TUSK_OTEL_ENDPOINT` in env | Spans ship to collector (advanced — skip if no collector) | |
| 7.5.3 | Long-running query → cancel button | Cancel endpoint fires `pg_cancel_backend`, query stops | |

## 8. Analytics (DuckDB)

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 8.1 | `/analytics` route | DuckDB query editor | |
| 8.2 | `SELECT 1` | Works, in-process | |
| 8.3 | Upload Parquet/CSV → query | File registered as table, queryable | |
| 8.4 | DuckDB pagination | Server-side (was added 0.3.0) | |

## 9. Cluster (tusk-cluster plugin)

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 9.1 | `/cluster` route | Shows worker list, job list | |
| 9.2 | Submit a distributed job | Splits across workers | |
| 9.3 | View Result — **task #49** | KNOWN BROKEN — endpoint missing | 🔴 |
| 9.4 | Retry job — **task #49** | KNOWN BROKEN — endpoint missing | 🔴 |
| 9.5 | DevTools console — **task #48** | KNOWN BROKEN — `tuskToast()` undefined (should be `showToast`) | 🔴 |

## 10. tusk-bi plugin (Dashboards)

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 10.1 | `/bi` Overview page | Stats, connected apps, query volume chart | |
| 10.2 | `/bi/dashboards` | List of dashboards | |
| 10.3 | Click dashboard → viewer | 3-panel layout, widgets render | |
| 10.4 | Live badge when `refresh_interval` set | Green pulse next to title | |
| 10.5 | Public badge when `is_public=true` | Visible badge | |
| 10.6 | Dashboard editor | 3-panel: widget library / canvas / config | |
| 10.7 | Add a widget (chart/table/stat) | Drops on canvas, configurable | |
| 10.8 | Save dashboard | Persists, reloads correctly | |

## 11. tusk-ci plugin

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 11.1 | `/ci` route | Plugin loads | |
| 11.2 | Pipeline list | Renders | |
| 11.3 | Run a pipeline manually | Status updates | |
| 11.4 | Note: this plugin is **slated for removal** per latest strategy. Just confirm what works, don't deep-dive. | | |

## 12. tusk-security plugin

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 12.1 | `/security` route | Findings list | |
| 12.2 | Run a scan | Generates findings | |
| 12.3 | Note: also slated for removal. Just confirm what works. | | |

---

## 13. CLI

Run from container shell:

| # | Command | Expected | Status |
|---|---------|----------|--------|
| 13.1 | `tusk --version` | `0.4.24` | |
| 13.2 | `tusk studio --help` | Help text | |
| 13.3 | `tusk ai stats --days 30` | Verdict breakdown table | |
| 13.4 | `tusk ai stats --days 1` | Recent prompts | |
| 13.5 | `tusk ai debug-prompt <conn_id> "tu pregunta"` | Prints the schema text that would be sent to LLM | |
| 13.6 | `tusk ai clear-memory` (if exists) | Truncates ai_memory.db | |
| 13.7 | `tusk backup <conn>` if CLI exists | Backups via CLI | |

---

## 14. Security checks (passive)

| # | Check | Expected | Status |
|---|-------|----------|--------|
| 14.1 | `sqlite3 /var/lib/tusk/.tusk/connections.db "select password from connections limit 1"` | Ciphertext starting with `gAAAA...` (Fernet), not plaintext | |
| 14.2 | Without auth → `/admin` | Redirects to login (multi-user) OR allowed only from loopback (single-user) | |
| 14.3 | Hit `/api/data/upload` 11 times in 60s | 11th rejected (rate limit 10/min) | |
| 14.4 | Hit `/api/data/export` 21 times in 60s | 21st rejected (rate limit 20/min) | |
| 14.5 | Audit log: did your export show up in audit DB? | Check `audit.db` table `events` for `data.export` row with your user | |
| 14.6 | `/api/ai/sql` with 5000-char prompt | 400 "prompt too long (max 4096)" | |
| 14.7 | AI generates `DROP TABLE ...` | Red banner above Insert/Replace | |

---

## 15. Stability / process resilience

| # | Action | Expected | Status |
|---|--------|----------|--------|
| 15.1 | Hit `/api/data/<huge query>` that takes >60s | Times out cleanly (RequestTimeoutMiddleware 300s budget for `/api/data/`) | |
| 15.2 | Hit a non-data slow endpoint | 60s default budget | |
| 15.3 | `docker logs` after a failed request | Structured error log present, not silent 500 | |
| 15.4 | Background job that hangs >10min | Watchdog marks it as TIMED_OUT (added 0.4.11) | |
| 15.5 | Kill granian worker — does it restart? | Granian recycles, requests succeed after | |

---

## 16. Pages with known JS errors (just confirm they still error)

| # | Page | Bug | Status |
|---|------|-----|-------|
| 16.1 | Data page | `_applyReproject` undefined — task #45 | |
| 16.2 | Any page using confirm/prompt | `tuskConfirm()` / `tuskPrompt()` undefined — task #46 | |

---

## 17. Notes section (write what you find)

Use this for everything that doesn't fit a row above:

- ...
- ...
- ...

---

## Summary table

Once done, tally:

- 🟢 working: ___ / ___
- 🟡 partial: ___ / ___
- 🔴 broken (excl. known): ___ / ___
- ⚪ skipped: ___ / ___

If 🔴 count is non-trivial, dump each one with steps + screenshot into `specs/bugs/2026-05-21-walkthrough-findings.md`. We file new tasks per row from there.

Then we decide: how many 🔴 must go to 🟢 before tagging 0.5.0-rc1.
